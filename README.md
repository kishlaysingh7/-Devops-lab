# -Devops-

Step 2: Run the script
Once the script has execution permission, you can run it using the command:

Step 2: Run the script
Once the script has execution permission, you can run it using the command:
./server-stats.sh

https://github.com/kishlaysingh7/-Devops-lab
